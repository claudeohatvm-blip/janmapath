# Core - required
Django==5.2.17
pyswisseph==2.10.3.2

# PDF export. Needs system libraries (Pango, Cairo):
#   Debian/Ubuntu  sudo apt install libpango-1.0-0 libpangoft2-1.0-0 libcairo2
#   macOS          brew install pango
#   Windows        see https://doc.courtbouillon.org/weasyprint/stable/first_steps.html
# Without it the app falls back to the browser's own print-to-PDF.
weasyprint==67.1

# Optional - AI narration. The reading is complete without this.
# See the "AI setup" page in the running app, or docs/AI_SETUP.md
# anthropic>=0.40

# Development
pytest==8.4.2
