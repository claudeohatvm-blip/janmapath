# Python 3.11 or newer.

# Core - required
Django>=5.2,<6.0
pyswisseph>=2.10,<3.0

# PDF export. Needs system libraries (Pango, Cairo):
#   Debian/Ubuntu  sudo apt install libpango-1.0-0 libpangoft2-1.0-0 libcairo2
#   macOS          brew install pango
#   Windows        https://doc.courtbouillon.org/weasyprint/stable/first_steps.html
# Without it the app falls back to the browser's own print-to-PDF, so this is
# optional in practice.
weasyprint>=62,<71

# Optional - AI narration. The reading is complete without either of these;
# see the "AI setup" page in the running app, or docs/AI_SETUP.md
# google-genai>=1.0          # Google Gemini, via AI Studio
# anthropic>=0.40            # Anthropic Claude

# Development
pytest>=8.0
