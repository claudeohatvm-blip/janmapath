# Copy to .env and fill in. Never commit the real file.
#
# Optional. Without it, readings are written from rules in English, and
# every placement, score and conclusion is identical. See the "AI setup"
# page in the running app for how to obtain a key.
ANTHROPIC_API_KEY=sk-ant-...
